* fix pygithub bug with due_on date format
* make PR for import API for pygithub
* write script to check and clean up label usage

document
* enable XML RPC and permissions for trac
* create user
 * create token
* create repo
 * enable issues
 * add contributors
* need to run twice because assignees fail in import but can be set later
